배열이란 다중의 요소를 저장하는 데이터 구조이다. 일반적으로는 하나의 타입을 저장할 수 있지만, 파이썬의 내장형은 list는 내부 요소의 동적 타입을 지원한다.

```python
x = [] # 빈 배열

y = [1, 2, 3, 4, 5] # 배열의 초기화

z = [1, "hello", 3.14, True] # 혼합 타입 배열의 초기화
```

배열에서 알고리즘의 시간 복잡도는 일반적으로 데이터 집합의 크기에 따라 선형으로 증가한다.

단, 특정 인덱스에 접근하는 시간 복잡도는 O(1)로 굉장히 빠르다.