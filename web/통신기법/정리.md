## 전체 프로토콜 한눈에 정리

지금까지 다룬 8가지 + 미처 다루지 못한 것들까지 한 번에 정리해드릴게요.이번엔 채팅으로 깔끔하게 정리해드리는 게 더 적합할 것 같습니다. 바로 시작할게요!

---

## 전체 프로토콜 비교 정리

### 📋 한눈에 보는 분류표

| 프로토콜 | 통신 패턴 | 전송 계층 | 주요 도메인 |
|----------|-----------|-----------|-------------|
| REST API | 요청-응답 | HTTP/1.1, 2 | 범용 웹 API |
| GraphQL | 요청-응답 | HTTP | 유연한 데이터 조회 |
| gRPC | 요청-응답 + 스트리밍 | HTTP/2 | 마이크로서비스 내부 통신 |
| WebSocket | 양방향 지속 연결 | TCP | 실시간 양방향 통신 |
| SSE | 서버→클라이언트 단방향 | HTTP | 실시간 알림, AI 스트리밍 |
| MQTT | Pub/Sub | TCP | IoT, 경량 디바이스 |
| AMQP | 메시지 큐 | TCP | 엔터프라이즈 메시징 |
| WebRTC | P2P | UDP | 영상/음성 통화 |
| CoAP | 요청-응답 | **UDP** | 초경량 IoT 센서 |
| XMPP | 메시지/프레젠스 | TCP | 채팅, 분산 메시징 |
| STOMP | 메시지 큐 | TCP/WebSocket | 브로커 기반 메시징 |

---

### 🎯 용도별로 고르는 방법

**일반적인 웹/모바일 API를 만든다면** → REST API가 가장 무난합니다. 생태계가 가장 크고 학습 비용이 낮습니다.

**프론트엔드가 다양하고 필요한 데이터 형태가 제각각이라면** → GraphQL이 적합합니다. 클라이언트가 원하는 것만 딱 받아올 수 있습니다.

**마이크로서비스끼리 빠르게 대량 통신해야 한다면** → gRPC가 최적입니다. Protobuf의 성능과 강타입 인터페이스가 강점입니다.

**채팅, 실시간 알림, 주식 시세처럼 서버가 먼저 데이터를 밀어줘야 한다면** → 단방향이면 SSE, 양방향이면 WebSocket을 선택합니다.

**ChatGPT처럼 AI 응답을 토큰 단위로 스트리밍한다면** → SSE가 구현이 단순하고 HTTP 인프라와 호환성도 좋아 딱 맞습니다.

**화상회의, 음성통화처럼 초저지연 P2P 미디어가 필요하다면** → WebRTC가 사실상 유일한 선택지입니다.

**주문, 결제처럼 메시지 유실이 절대 안 되는 비동기 처리라면** → AMQP(RabbitMQ)가 적합합니다. 강력한 ACK와 라우팅을 제공합니다.

**스마트홈, 센서처럼 배터리/대역폭이 극히 제한된 IoT라면** → MQTT 혹은 더 극단적인 경우 CoAP을 사용합니다.

---

### 🔗 함께 쓰이는 조합들

실제 서비스에서는 하나만 쓰기보다 여러 프로토콜을 **역할에 맞게 혼합**해서 사용하는 경우가 많습니다.

**REST + WebSocket** 조합은 가장 흔합니다. 일반 CRUD는 REST로, 실시간 알림은 WebSocket으로 처리합니다.

**REST + SSE** 조합은 AI 서비스에서 자주 보입니다. 설정 API는 REST, 응답 스트리밍은 SSE로 처리합니다.

**gRPC + AMQP** 조합은 마이크로서비스 아키텍처에서 많이 쓰입니다. 동기 통신은 gRPC, 비동기 이벤트는 AMQP로 분리합니다.

**WebRTC + WebSocket** 조합은 화상회의 서비스의 전형적인 패턴입니다. 미디어 스트림은 WebRTC P2P로, 시그널링과 채팅은 WebSocket으로 처리합니다.
